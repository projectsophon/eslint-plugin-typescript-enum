import noConstEnum from "./no-const-enum";
import noEnum from "./no-enum";
import onlyConstEnum from "./only-const-enum";

const index = {
  "no-const-enum": noConstEnum,
  "no-enum": noEnum,
  "only-const-enum": onlyConstEnum
};

export default index;
